<div class="authform">
  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
    <h3>Connexion</h3>
    <%= devise_error_messages! %>
    <div class="form-group">
      <%- if devise_mapping.registerable? %>
        <%= link_to 'Créer un compte', new_registration_path(resource_name), class: 'right'  %>
      <% end -%>
      <%= f.label :email %>
      <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%- if devise_mapping.recoverable? %>
        <%= link_to "Mot de passe oublié?", new_password_path(resource_name), class: 'right' %>
      <% end -%>
      <%= f.label "Mot de passe" %>
      <%= f.password_field :password, class: 'form-control' %>
    </div>
      <%= f.submit 'Me connecter', :class => 'button right' %>
      <% if devise_mapping.rememberable? -%>
        <div class="checkbox">
          <%= f.check_box :remember_me %> <%= f.label "Se souvenir de moi" %>
        </div>
      <% end -%>
  <% end %>
</div>
